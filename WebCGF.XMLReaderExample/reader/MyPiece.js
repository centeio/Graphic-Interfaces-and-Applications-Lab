function MyPiece(scene, player, name, piece){
    this.scene = scene;
    this.player = 0;
    this.name = "name";
    this.piece = piece;
}

MyPiece.prototype.display = function(){
 /*   console.log("piece --- ");
    console.debug(this.piece);
    this.piece.display();*/
}